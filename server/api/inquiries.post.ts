type InquiryBody={name?:string;email?:string;company?:string;phone?:string;service?:string;package?:string;launch?:string;budget?:string;description?:string;consent?:boolean;website?:string;startedAt?:number}
const attempts=new Map<string,{count:number;reset:number}>();
export default defineEventHandler(async event=>{const body=await readBody<InquiryBody>(event);if(body.website)throw createError({statusCode:400,statusMessage:'Invalid submission.'});if(!body.startedAt||Date.now()-body.startedAt<2500)throw createError({statusCode:429,statusMessage:'Please take a moment to review your inquiry before sending.'});const ip=getRequestIP(event,{xForwardedFor:true})||'unknown';const now=Date.now(),current=attempts.get(ip);if(!current||current.reset<now)attempts.set(ip,{count:1,reset:now+60*60*1000});else{current.count++;if(current.count>5)throw createError({statusCode:429,statusMessage:'Too many inquiries. Please try again later.'})}const name=String(body.name||'').trim(),email=String(body.email||'').trim(),description=String(body.description||'').trim();if(name.length<2||name.length>120||!/^\S+@\S+\.\S+$/.test(email)||description.length<20||description.length>1500||!body.consent)throw createError({statusCode:422,statusMessage:'Please complete all required fields.'});const config=useRuntimeConfig(event);if(!config.supabaseUrl||!config.supabasePublishableKey)throw createError({statusCode:503,statusMessage:'Inquiry storage is not configured.'});const record={name,email,company:String(body.company||'').trim().slice(0,160)||null,phone:String(body.phone||'').trim().slice(0,40)||null,service:String(body.service||'').slice(0,50),package:String(body.package||'unsure').slice(0,30),launch_timeline:String(body.launch||'').slice(0,60)||null,budget:String(body.budget||'').slice(0,40)||null,description,consent:true};try{await $fetch(`${config.supabaseUrl}/rest/v1/project_inquiries`,{method:'POST',headers:{apikey:String(config.supabasePublishableKey),Authorization:`Bearer ${config.supabasePublishableKey}`,Prefer:'return=minimal'},body:record})}catch{throw createError({statusCode:502,statusMessage:'We could not save your inquiry. Please try again shortly.'})}return {ok:true}})
